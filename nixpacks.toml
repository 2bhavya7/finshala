[providers]
node = "20"
python = "3.11"

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r backend/python_api/requirements.txt"
]
